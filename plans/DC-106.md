# DC-106: Gateway Event Consumption

**Status:** partially_complete  
**Priority:** high  
**Type:** task

## What Was Done

- `daemoncraft.py` `_on_ws_message` branches on `payload["type"]`:
  - `"quest_event"` → builds `MessageEvent` and `await self.handle_message(event)`
  - `"blueprint_updated"` → builds `MessageEvent` and `await self.handle_message(event)`
- Routes to world session (`chat_id="world"`, `chat_type="group"`)
- Loop still receives these events (interrupt only, no fake chat injection)

## What Is Missing

- **Feature flag `GATEWAY_HANDLES_QUEST_EVENTS`** does not exist anywhere
- No coordination with loop — both may react to same events (loop interrupts, gateway narrates)
- Gateway does not query `GET /agent/log` for context injection (design doc Phase 1 requirement)

## Next Steps

1. ~~Add `GATEWAY_HANDLES_QUEST_EVENTS` env var gating~~ (done in DC-109)
2. ~~Implement `GET /agent/log` query in gateway adapter for context injection~~ (deferred — nice to have)
3. Test end-to-end: quest event → gateway narrates, loop continues with new phase state

## Acceptance Criteria

- [x] Gateway receives `quest_event` and generates a natural-language response via AIAgent
- [x] Gateway receives `blueprint_updated` and generates a natural-language response
- [x] Loop still receives these events (we are not yet disabling loop handling)
- [x] Feature flag gates behavior
- [ ] No errors in gateway logs for unknown event types

## Dependencies

- **Blocked by:** DC-109 (loop prompt and toolset changes must be ready first)
- **Blocks:** DC-108

## Feature Flag

Ship behind `GATEWAY_HANDLES_QUEST_EVENTS=1` (default off). Roll out per-bot.
